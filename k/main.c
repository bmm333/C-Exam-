#include <stdio.h>
#include <stdlib.h>
#include "lib.h"
#include <stdbool.h>

int main(int argc,char* argv[])
{
	if(argc!=2)
	{
		fprintf("Usage %s:<filename>",argv[0]);
		exit(EXIT_FAILURE);
	}
	/*Still to work*/
}